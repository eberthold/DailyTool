<UserControl
    x:Class="DailyTool.UserInterface.Initialization.AddPersonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DailyTool.UserInterface.Initialization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DailyTool.ViewModels.Initialization"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    d:DataContext="{d:DesignInstance Type=vm:AddPersonViewModel, IsDesignTimeCreatable=False}">

    <StackPanel Padding="24" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock Text="TODO: Person hinzufügen" Style="{ThemeResource SubheaderTextBlockStyle}"/>

        <TextBox x:Name="PART_Name" Header="TODO: Name" Margin="0, 12" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="TODO: Hinzufügen" Command="{Binding AddPersonCommand}" />
            <Button Content="TODO: Abbrechen" Command="{Binding CancelCommand}" />
        </StackPanel>
    </StackPanel>
</UserControl>
