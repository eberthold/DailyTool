<Page
    x:Class="DailyTool.UserInterface.Initialization.InitializationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DailyTool.UserInterface.Initialization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DailyTool.ViewModels.Initialization"
    xmlns:toolkit="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:resources="using:DailyTool.UserInterface.Resources"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:InitializationViewModel, IsDesignTimeCreatable=False}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <!-- Add only for design time -->
    <Page.Resources>
        <ResourceDictionary>
            <d:ResourceDictionary.MergedDictionaries>
                <resources:ModuleResourceDictionary />
            </d:ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="TODO: Vorbereitung" Style="{StaticResource TitleTextBlockStyle}" />

        <toolkit:DataGrid ItemsSource="{Binding CurrentState.Peoples, Mode=OneWay}">
            <toolkit:DataGrid.Columns>
                <toolkit:DataGridCheckBoxColumn Header="TODO: IsSelected" />
                <toolkit:DataGridTemplateColumn Header="TODO: IsParticipating"  CellTemplate="{StaticResource IsParticipatingCellTemplate}"/>
                <toolkit:DataGridTextColumn Header="TODO: Name" Binding="{x:Bind Name}" />
            </toolkit:DataGrid.Columns>
        </toolkit:DataGrid>
    </Grid>
</Page>
