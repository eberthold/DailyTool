<Page x:Class="DailyTool.UserInterface.Settings.SettingsOverviewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:DailyTool.UserInterface.Settings"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <Grid Padding="24 12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="150" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>

        <ListView x:Name="PART_SettingsMenu" ItemsSource="{Binding Settings}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border Grid.Column="1">
            <ContentControl MinWidth="80"
                            MinHeight="80"
                            Content="{Binding ElementName=PART_SettingsMenu, Mode=OneWay, Path=SelectedItem}"
                            ContentTemplateSelector="{StaticResource SettingsTemplateSelector}" />
        </Border>
    </Grid>
</Page>
