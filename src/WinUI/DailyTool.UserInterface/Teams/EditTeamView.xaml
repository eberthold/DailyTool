<UserControl x:Class="DailyTool.UserInterface.Teams.EditTeamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="using:DailyTool.ViewModels.Teams"
             d:DataContext="{d:DesignInstance Type=viewModel:EditTeamViewModel,
                                              IsDesignTimeCreatable=False}"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="4" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="TODO: Name des Teams" />
            <TextBox Text="{Binding Team.Name, Mode=TwoWay}" />
        </StackPanel>

        <StackPanel Grid.Row="2"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Command="{Binding SaveCommand}" Content="TODO: Speichern" />
            <Button Command="{Binding CancelCommand}" Content="TODO: Abbrechen" />
        </StackPanel>
    </Grid>
</UserControl>
